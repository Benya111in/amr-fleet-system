image: warehouse.pgm
mode: trinary
resolution: 0.05
origin: [-30.1783, -20.1938, 0]
negate: 0
occupied_thresh: 0.65
free_thresh: 0.19
# 재매핑 2026-09-22 (통합 트리, LiDAR 스캔 평면 지면 +0.20 m, slam_toolbox fine_search_angle_offset 0.00087):
# system.launch.py localization_mode:=slam, 시작 자세 월드 (0, 0, 0) (GT 확인) → GT 되먹임 주행 322 m.
# map_saver_cli 원점 (-30.2, -20.2) 에 map_quality 정렬 이동량 (+0.0217, +0.0062) m 를 더해 map 프레임 = 월드 좌표
# (남은 회전 -0.029° 는 맵 끝에서 반 셀 미만이라 재표본화하지 않음). free_thresh 0.19 < 0.196: 미지(205)를 미지로
# 읽는다 (nav2 map_saver 기본 YAML 의 0.25 는 미지를 자유로 읽는다). 품질·절차: docs/algorithms/slam.md §6.1
