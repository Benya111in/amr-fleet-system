# pip 제약 파일 — docker/Dockerfile 의 pip 고정 버전과 같은 값. 이미지의 ENV PIP_CONSTRAINT 가 가리키므로
# Dockerfile 의 pip 설치는 물론 컨테이너 안의 ad-hoc `pip install ...` 도 아래 패키지는 이 버전을 벗어나지 못한다
# (예: `pip install -U ultralytics` 가 opencv-python 을 다시 깔아 contrib 의 cv2.aruco 를 지우거나 numpy 2 가 들어오는 사고).
# 버전을 바꾸려면 Dockerfile 의 pip 층과 이 파일을 함께 고치고 이미지를 다시 빌드한다.
numpy==1.26.4
opencv-python==4.11.0.86
opencv-contrib-python-headless==4.11.0.86
ultralytics==8.4.155
transforms3d==0.4.2
scipy==1.15.3
matplotlib==3.5.1
pandas==2.3.3
pytest==8.3.5
pytest-cov==7.1.0
pytest-timeout==2.4.0
flask==3.1.3
flask-socketio==5.6.1
eventlet==0.41.2
pyyaml==5.4.1
jsonschema==4.26.0
torch==2.11.0
torchvision==0.26.0
