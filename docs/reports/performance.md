# 성능 측정 및 분석 리포트

> 명세 10장 요구 산출물. 측정 절차·도구의 단일 출처는 [docs/reports/README.md](README.md),
> 시나리오 정의·판정 기준은 [tests/integration/README.md](../../tests/integration/README.md) 와
> `tests/integration/amr_itest/catalog.py`, 시나리오별 실행 이력은
> [integration_tests.md](integration_tests.md) 다.
>
> **이 표를 읽는 법**: 모든 수치는 아래 환경에서 **실제로 돌려 얻은 값**이고, 각 행에 출처(시나리오 ·
> 로그 파일)와 표본 수를 적었다. 시나리오 안의 반복(08 의 30 시행 등)은 표본 수로 드러나지만,
> **시나리오 자체는 1회 실행**이다 — 반복 측정에 따른 신뢰구간은 없다. 전부 시뮬레이션 값이며 센서
> 잡음은 우리가 모델링한 것이다 (`config/sensors.yaml`).

## 1. 측정 환경

| 항목 | 값 |
| --- | --- |
| 호스트 | 32 코어, NVIDIA RTX 5090 (드라이버 580.142), Linux 4.18 |
| 컨테이너 | `amr-fleet-system:wf-final` (ROS 2 Humble, Gazebo Fortress 6.18), `--gpus all --shm-size=8g` |
| 워크스페이스 | `feature/system-integration` (측정 시점 커밋은 각 절에 표기) |
| 실행 | `./scripts/run_integration.sh --timeout-scale 2 [--scenario-timeout N] <시나리오>` |
| 결과 | `logs/itest/<시나리오 id>/{result.json, junit.xml, launch.log, *.csv}` |

`--timeout-scale 2` 는 **대기 상한만** 2배로 늘린다 (측정값 자체는 바뀌지 않는다). 공유 서버라
실행마다 호스트 부하가 달랐고(loadavg 3.9 ~ 60), 각 표의 마지막 열에 실행 시점 부하를 적었다.

## 2. 명세 지표 요약

| 명세 지표 | 목표 | 측정 | 표본 | 출처 | 판정 |
| --- | --- | --- | --- | --- | --- |
| 위치 추정 RMSE (정지/직선/회전) | 3 / 5 / 8 cm | **1.47 / 1.32 / 1.44 cm** | 825 / 416 / 617 | 04, `pose_error.csv` | 충족 |
| 경로 추종 CTE (직선/곡선) | 5 / 10 cm | **2.87 / 3.12 cm** | 1032 / 459 | 07, `cte.csv` | 충족 |
| 경로 계획 성공률 (임의 50쌍) | ≥ 98 % | **100 %** (50/50) | 50쌍 | 06 | 충족 |
| 경로 계획 시간 (A\*) | — (재계획 500 ms 예산 안) | 평균 3.9 · **최대 13.0 ms** | 50 | 06 | 충족 |
| 동적 장애물 충돌 | 30회 중 0건 | **1건** (로봇 정지 중, 침투 6.9 mm — §4.5) | 30 시행 | 08, `contacts.csv` | 미달 |
| 회피 시 경로 이탈 | ≤ 1 m | **0.448 m** | 30 시행 최댓값 | 08 | 충족 |
| 원경로 복귀 시간 | ≤ 5 s | **2.5 s** | 30 시행 최댓값 | 08 | 충족 |
| 도킹 정밀도 | 2 cm / 1° | **10/10 성공** | 10 시행 | 10 | 충족 |
| 긴급 정지 (0.3 m 접근) | 즉시 정지 | 정지 여유 **0.317 m**, 최소 여유 0.311 m | — | 09 (gazebo) | 충족 |
| 안전 반응 지연 | (설계 20 ms) | 스캔→정지 **0.39 ms** | 최댓값 | 09 (gazebo) | 충족 |
| E-stop 버튼 → 정지 | 즉시 | **0.82 ms** (`/fleet/estop` 0.35 ms) | 최댓값 | 09 | 충족 |
| SLAM 지도 해상도 | ≤ 0.05 m | **0.05 m** | — | 03 | 충족 |
| 지도 품질 (재현율 / 오점유) | 지표 정의·측정 | **99.6 % / 0.19 %** | 20000 표본점 | 03 | 충족 |
| 5대 동시 운용 CPU | ≤ 80 % | **58.1 %** (최대 66 % 대, 호스트 전체 69 % 대) | 373+ 표본 | 12, `cpu.csv` | 충족 |
| 테스트 커버리지 | ≥ 70 % | **95 %** (Python 문장 기준) | 14545 문장 | `colcon test` | 충족 |
| 통합 시나리오 자동화 | ≥ 10개 | **14개** | — | `catalog.py` | 충족 |
| 시스템 응답 시간 | ≤ 200 ms | **평균 136.7 ms** | 50 | 13, `response_time.csv` | 충족 |
| 연속 운용 | 4시간 안정성 | **4.01 h, 크래시 0, 누수 0** / 작업 실패율 5.26 % | 작업 38건 | 14 | 안정성 충족, 실패율 미달 |
| 작업 성공률 | ≥ 97 % | 14: **94.7 %** (36/38, 외부 부하 구간 TIMEOUT 2) · 12: **100 %** (5/5) | 38 + 5 | 14 · 12 | §4.3 |

**커버리지 범위**: 95 %는 `colcon coveragepy-result` 의 Python 문장 커버리지다. C++ 패키지는 gtest
로 시험하지만 gcov 를 구성하지 않아 줄 커버리지 수치가 없다 — 명세의 "주요 모듈 70 %"는 Python
모듈 기준으로 충족한다.

## 3. 시나리오별 결과

| # | 시나리오 | 구성 / 백엔드 | 결과 | 주요 수치 | 실행 부하 |
| --- | --- | --- | --- | --- | --- |
| 01 | 센서 토픽 | component / gazebo | 통과 | LiDAR 10.0 Hz·720점·0.5°·σ 2.97 cm, RGB 30.3 Hz, depth 15.15 Hz·σ 4.9 mm, IMU 100 Hz, GT 50 Hz | 2.7 |
| 02 | TF 트리 | component / gazebo | 통과 | 계약 간선 전부, 이중 부모·순환 0, 뿌리 map | 2.9 |
| 03 | SLAM 지도 | system / gazebo | 통과 | 재현율 99.6 %, 오점유 0.19 %, 정합 3.11 cm / −0.063°, 저장 성공 | 3.9 |
| 04 | EKF 정확도 | system / gazebo | 통과 | RMSE 1.47 / 1.32 / 1.44 cm (amr_evaluation 1.43 / 1.32 / 1.44) | 5.5 |
| 05 | 납치 복구 | system / gazebo | 통과 | 5/5, 검출 3.06~3.50 s, 복구 4.25~4.72 s | 5.3 |
| 06 | 경로 계획 | system / gazebo | 통과 | AStar 50/50 (NavFn 50/50, Smac 45/50), 최대 13.0 / 19.0 / 44.0 ms | 7.4 |
| 07 | 경로 추종 | system / gazebo | 통과 | CTE 직선 2.87 cm, 곡선 3.12 cm, 목표 5/5 | 12.4 |
| 08 | 동적 장애물 | system / gazebo | 부분 | 30 시행, 접촉 1(정지 중), 이탈 0.448 m, 복귀 2.5 s, 도달 30/30, 조우 30/30 | 6.3 |
| 09 | 긴급 정지 | component / kinematic · gazebo | 통과 · 통과 | 주입→정지 99.98 ms (kinematic), 스캔→정지 0.39 ms·여유 0.317 m (gazebo) | 5.1 / 5.2 |
| 10 | 정밀 도킹 | system / gazebo | 통과 | 10/10 (2 cm / 1°), 마커 가림 시 3회 소진 후 실패 보고 | 12.0 |
| 11 | BT 복구 | system / gazebo | 2/3 | 인식 12.6 s·차단 1.0 s 복구 후 COMPLETED, 위치 상실 71.7 s (기준 60 s) | 12.7 |
| 12 | 5대 교착 | system / gazebo | 통과 | 5대, 작업 5/5, 자연 교착 탐지 0, 강제 교착 1 탐지·1 해소, CPU 58.1 % | 55.8 |
| 13 | 응답 시간 | system / gazebo | 통과 | 표본 50, 벽시계 평균 **136.7 ms** (전체 회귀 실행), 무응답 0 | 12.7 |
| 14 | 연속 운용 | system / gazebo | 부분 | 4.01 h, 크래시 0, 누수 0, 완료 36 (평균 216 s), TIMEOUT 2 → 실패율 5.26 % | 9.4 (시작 7.2, 중간 123) |

## 4. 미달·열린 항목

### 4.1 시나리오 11 — 위치 상실 복구 71.7 s (기준 60 s)

같은 형태의 평행 통로로 6 m 순간 이동시키면 옛 추정 자세에서도 스캔이 그대로 맞아
(**지각적 aliasing**) 공분산·점프·인라이어 어디에도 걸리지 않는다. 즉 "위치를 잃었다"는 신호가
센서에서 나오지 않는다. 지도에서 유일한 ArUco 마커로 이 애매함을 끊었다 (§5 참고):

| 구간 | 시간 |
| --- | --- |
| 순간 이동 → 마커 최초 관측 | 약 57 s (로봇이 도크 근처까지 주행해야 마커가 시야에 든다) |
| 마커 보정 → 수렴 완료 | **14.3 s** |

초과분은 복구 알고리즘이 아니라 **랜드마크가 시야에 들어오기까지의 주행 거리**다. ArUco 검출기는
이미 항상 켜져 있어 더 일찍 켤 여지가 없고, 줄이려면 통로에 랜드마크를 늘려야 한다 (월드·지도
변경 → 03·06~08 재검증 필요). 명세에는 납치 복구 시간 제한이 없고, 명세용 납치 시나리오인 05 는
5/5 통과(검출 3.2 s, 복구 4.5 s)다. 60 s 는 이 프로젝트가 세운 기준이다.

### 4.2 시나리오 12 — 교착 해소 (해결됨)

처음 측정에서 교착 6~7건 중 해소 1건 이하였다. 교착 이벤트에 **탐지 시 로봇 좌표**를 싣도록 고친
뒤 원인이 드러났다 — 7건이 전부 `(-28, 17)`, 즉 dock_1 staging 한 자리였다. 통로 교착이 아니라
**같은 도크로 세 대가 몰린 자원 경합**이었고, 세 대 모두 '목표 자리' 에 있으니 비켜설 포켓도 목표를
피하는 대체 경로도 의미가 없었다. 세 가지를 고쳤다:

1. **도크 접근 구역 4개를 용량 1 구역으로 등록** (기존 토큰 메커니즘) — 같은 도크에 두 대가 들어가지
   못하게 예방한다. 자연 발생 교착이 0 건이 됐고 작업도 5/5 완료.
2. **1차선·막다른 구간에서는 구역 안으로라도 물러선다** (`pocket_into_zone`) — 폭이
   `pocket_clearance_m` 보다 좁으면 '구역 밖' 포켓 후보가 전부 걸러지기 때문이다 (단위 시험으로 재현).
3. **ALT_PATH 희생 로봇의 토큰 hold 해제** — 실행기는 교통 hold 동안 주행 goal 을 취소하므로
   (`move_to.xml`), 마스크를 주고도 세워 두면 재계획 자체를 못 해 "마스크를 피하는 경로"가 영영
   나오지 않았다. 마스크가 이미 막는 구역에 한해 hold 를 걸지 않는다 (관측이 끊긴 보유자에는
   fail-closed 로 hold 유지). 이 수정 뒤 강제 교착이 **탐지 1 · 해소 1** 로 통과했다.

### 4.3 시나리오 14 — 작업 실패율 5.26 % (기준 2 %)

4시간 동안 **크래시 0, 메모리 누수 0**(워밍업 제외 기울기 5 MB/h 미만, 전 프로세스 판정 완료),
완료 36건(평균 216 s)이다. 실패 2건은 모두 TIMEOUT 이고, 발생 시각(sim 10170 s · 11693 s)은
**다른 사용자 작업으로 호스트 부하가 123 까지 오른 구간**과 겹친다 — 그 구간에서 우리 YOLO 는
30 FPS 에서 0.5 FPS 로 떨어졌고 작업 하나가 801 s (평소 216 s) 걸렸다. 노드는 그 동안에도 하나도
죽지 않았다.

즉 이 미달은 시스템 불안정이 아니라 **자원 경합에 따른 처리량 저하**다. 호스트가 조용해진 뒤
재측정 중이며, 결과가 나오면 두 실행을 나란히 싣는다.

이전 캠페인에서 "planner_server 151 MB/h 누수"로 의심했던 항목은 이번 4시간 측정으로 **누수가
아님**이 확인됐다: 워밍업 10분을 제외한 기울기가 5 MB/h 미만이고, 중간에 감소 구간(−10.5 MB/h)도
관측된다 — rcl_action 이 결과를 15분간 보관했다가 회수하는 동작과 맞는다.

### 4.5 시나리오 08 — 잔여 접촉 1건 (로봇 정지 중)

회피 자체는 명세 여유 안에서 동작한다 (이탈 0.448 m ≤ 1 m, 복귀 2.5 s ≤ 5 s, 도달 30/30).
남은 접촉 1건은 **로봇이 멈춰 선 상태에서 무작위 보행 작업자가 걸어와 닿은 것**이다 (침투 6.9 mm,
GT 로봇 속도 0.000 m/s, 차선 가로 0.53 m). 하네스가 접촉마다 남기는 귀속 기하(`contacts.csv`)로
확인한 값이며, 이 시나리오의 접촉은 모두 같은 성격이었다 — **로봇이 장애물로 주행해 들어간 접촉은
한 건도 없다**.

원인은 월드의 작업자(actor)가 로봇을 피하지 않는다는 점이다. 스크립트 배우는 정지한 로봇을 그대로
통과하려 하므로, 로봇이 어디에 서 있든 접촉이 날 수 있다. 이 때문에 회피 정책을 다음 순서로
보수화했고 접촉이 27 → 1 건으로 줄었다:

1. 횡단 통로 **밖** 가상 정지선 (crossing_yield)
2. 이미 들어섰으면 **뒤로 빠져나가기** (정지·후진 중에만, 경로 방향으로만)
3. 통로 반폭 여유 0.5 → 0.7 m, "먼저 빠져나간다" 판정 여유 1.0 → 2.0 s

더 줄이려면 배우가 로봇을 피하도록 월드를 바꾸거나(시나리오 전제 변경), 로봇이 통로 근처에 아예
서지 않도록 더 크게 우회해야 한다 (이탈 예산과 상충).

### 4.6 관측된 다른 결함 (고치고 재측정한 것)

| 증상 | 원인 | 조치 |
| --- | --- | --- |
| 08 접촉 27건 (모두 로봇 정지 중) | VO/TTC 는 "설 수 있는 속도"만 지켜 로봇이 횡단 통로 **안에서** 멈췄다 | 횡단 통로 밖 가상 정지선(crossing_yield) → 접촉 0 |
| 5대 기동 실패 | Nav2 lifecycle 관리자가 `change_state` 응답 유실 시 무한 대기 | lifecycle_watchdog 이 노드를 직접 전이, 하네스는 노드 상태로 판정 |
| 03 지도 저장 실패 | slam_toolbox 는 `/map` 에 내는데 내부 nav2 map_saver 는 `<ns>/map` 을 구독 (이름공간 아래에서 항상 실패) | slam 모드에 `map_saver_server` 를 띄우고 `map:=/map` |
| 07 곡선 CTE 표본 0 | CTE 게이트가 1 Hz 재계획을 가정했는데 BT 는 경로 무효 시에만 재계획 | 항법 상태가 있으면 `plan_timeout` 미적용 |
| 11 세 사례 모두 실패 | 검출되지 않은 납치가 뒤 사례까지 오염 | 마커 기반 재위치추정 |

## 5. 마커 기반 재위치추정 (신규)

도킹 서버가 **다른 도크의 마커**를 보면 (`perception/dock_marker_id` 가 기대 id 와 다르면) 그
마커의 지도 자세로 로봇 자세를 역산해 `localization/marker_fix` 로 낸다:

```
T_map_base = T_map_marker ∘ (T_base_marker)⁻¹
```

`kidnap_monitor_node` 는 이 자세가 현재 추정과 1 m 이상 어긋나면 LOST 를 선언하고 그 자세를 AMCL ·
map EKF 에 시드한다 (전역 가설 탐색을 건너뛰므로 별칭 가설로 다시 수렴할 위험이 없다). 오검출로
위치 추정을 흔들지 않게 6 m 이내에서 연속 3회 관측이 0.30 m 안에 들어올 때만 내고, 공분산은 거리에
비례해 키운다. 실측(시나리오 11): 3.18 m 에서 마커 0 을 보고 자세 (−27.12, 15.65, −168.8°) 를
σ 0.16 m 로 발행 → 14.3 s 만에 수렴.

## 6. 재현 방법

```bash
# 전체 (14 제외)
docker run --rm --gpus all --shm-size=8g -v "$PWD:/ros2_ws" amr-fleet-system:wf-final \
  bash -lc 'cd /ros2_ws && ./scripts/run_integration.sh --timeout-scale 2'

# 개별 (예: 08 동적 장애물 30 시행)
./scripts/run_integration.sh --domain-id 44 --timeout-scale 2 8

# 응답 시간 50 표본 · 5대 교착은 시나리오 상한을 늘린다
./scripts/run_integration.sh --scenario-timeout 7200 13
./scripts/run_integration.sh --scenario-timeout 5400 12

# 4시간 연속 운용
./scripts/run_integration.sh --include-long --soak-hours 4 14
```

단위 테스트·커버리지는 `./scripts/test.sh` (결과 `logs/coverage/`), 하네스 단위 테스트는
`./scripts/run_integration.sh --unit-only --coverage` 다.
